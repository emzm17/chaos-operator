---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: chaosexperiments.chaos.chaos.example.com
spec:
  group: chaos.chaos.example.com
  names:
    kind: ChaosExperiment
    listKind: ChaosExperimentList
    plural: chaosexperiments
    singular: chaosexperiment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ChaosExperiment is the Schema for the chaosexperiments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ChaosExperimentSpec defines the desired state of ChaosExperiment
            properties:
              cpuStress:
                description: CPU stress configuration (for cpu-stress type)
                properties:
                  load:
                    description: Load percentage per worker
                    type: integer
                  workers:
                    description: Number of CPU workers
                    type: integer
                required:
                - load
                - workers
                type: object
              duration:
                type: string
              interval:
                description: |-
                  Interval between chaos rounds (e.g., "30s", "1m")
                  If not set, chaos runs only once
                type: string
              memoryStress:
                description: Memory stress configuration (for memory-stress type)
                properties:
                  memory:
                    description: Amount of memory to consume (e.g., "256M", "512M",
                      "1G")
                    type: string
                  workers:
                    description: Number of workers to spawn
                    type: integer
                required:
                - memory
                - workers
                type: object
              networkDelay:
                description: Network delay configuration (for network-delay type)
                properties:
                  jitter:
                    description: Jitter in milliseconds
                    type: string
                  latency:
                    description: Latency in milliseconds
                    type: string
                required:
                - latency
                type: object
              percentage:
                description: Percentage of pods to affect (1-100)
                type: integer
              selector:
                additionalProperties:
                  type: string
                type: object
              targetNamespace:
                type: string
              type:
                description: Foo is an example field of ChaosExperiment. Edit chaosexperiment_types.go
                  to remove/update
                type: string
            required:
            - duration
            - selector
            - type
            type: object
          status:
            description: ChaosExperimentStatus defines the observed state of ChaosExperiment
            properties:
              affectedPods:
                description: Affected pods
                items:
                  type: string
                type: array
              chaosRounds:
                description: Number of chaos rounds executed
                type: integer
              endTime:
                description: End time of the experiment
                format: date-time
                type: string
              lastChaosTime:
                description: Last chaos execution time
                format: date-time
                type: string
              message:
                description: Message with additional information
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed ChaosExperiment spec
                format: int64
                type: integer
              phase:
                description: Phase of the experiment (Pending, Running, Completed,
                  Failed)
                type: string
              startTime:
                description: Start time of the experiment
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
